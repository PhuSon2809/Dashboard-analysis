import React from 'react';

const InfoUser = () => {
  return (
    <React.Fragment>
      <div className="w-full h-full">InfoUser</div>
    </React.Fragment>
  )
}

export default React.memo(InfoUser);