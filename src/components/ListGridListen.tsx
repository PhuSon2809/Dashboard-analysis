import React from "react";

const ListGridListen  = () => {
  return (
    <React.Fragment>
      <div className="w-full h-full">ListGridListen</div>
    </React.Fragment>
  )
}

export default React.memo(ListGridListen);